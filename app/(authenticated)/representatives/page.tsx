/**
 * `/representatives` — search by address + browse-all index.
 *
 * Thin server-component shim that re-exports the interactive client
 * component. Metadata is declared in the sibling `layout.tsx`.
 */

export { default } from '@/components/representatives/RepresentativesPage'
