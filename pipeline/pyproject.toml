[project]
name = "beyond-the-ballot-pipeline"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "supabase>=2.0",
  "httpx>=0.27",
  "anthropic>=0.40",
  "python-dotenv>=1.0",
  "lxml>=5.0",
  "tqdm>=4.66",
]

[tool.uv]
dev-dependencies = []
